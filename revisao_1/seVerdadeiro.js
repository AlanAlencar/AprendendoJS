/**
 * seVerdadeiro.js
 * @author Alan Alencar
 * @description Comportamento do if (condicional)
 */

// if é uma instrução condicional (pergunta por algo)

if ('1' === '1' || 1 === 2) { // sempre que for verdadeiro.
    console.log('É verdadeiro');
} else { // for falso.
    console.log('É falso');
}
