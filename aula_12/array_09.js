/**
 * array_09.js
 * @description criar um array e percorrer com "forEach" V3
 */

function exibe(car) {
    console.log(car);
}

const cars = ['honda', 'toyota', 'peugeot', 'volks', 'ford', 'chevrolet'];
// usando forEach
cars.forEach(exibe);