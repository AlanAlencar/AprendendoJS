/**
 * array_16.js
 * @description sortear um array, ordenar crescente de números
 */

const numeros = [10, 4, 8, 2, 99, 62, 3, 2];
console.table(numeros);
numeros.sort(); // trabalha com string, logo vai dar errado.
console.table(numeros);
