/**
 * array_12.js
 * @description criar um array e excluir do final com pop
 */

const cars = ['honda', 'toyota', 'peugeot'];
console.table(cars);
cars.pop(); // apaga no final do array.
console.table(cars);