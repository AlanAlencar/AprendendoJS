/**
 * array_06.js
 * @description criar um array e percorrer com "for of".
 */

const cars = ['honda', 'toyota', 'peugeot', 'volks', 'ford', 'chevrolet'];
// usando for of.
for (const car of cars) { // singular do plural. 
    console.log(car);
}