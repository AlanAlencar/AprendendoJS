/**
 * array_13.js
 * @description criar um array e excluir no inicio com shift
 */

const cars = ['honda', 'toyota', 'peugeot'];
console.table(cars);
cars.shift(); // apaga no inicio do array.
console.table(cars);